import { render } from "@testing-library/react";
import React from "react";
import App from "../App";

test("renders the App component", () => {
  render(<App />);
  // Aquí puedes realizar las aserciones y verificar el comportamiento esperado del componente renderizado
});
